# Railway Deploy

See the full guide in the monorepo: `docs/RAILWAY-DEPLOY.md`

Quick checklist:
- Set `PORT=3001`, `DATABASE_URL` (…/identity_db), `JWT_SECRET`, `INTERNAL_SERVICE_KEY`, `NODE_AUTH_TOKEN`
- Health: `GET /health`
